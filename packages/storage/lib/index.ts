import { createStorage, StorageType, type BaseStorage, SessionAccessLevel } from './base';
import { themeStorage } from './themeStorage';

export { themeStorage, createStorage, StorageType, SessionAccessLevel, BaseStorage };
